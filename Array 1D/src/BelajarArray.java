public class BelajarArray {
    public static void main(String[] args) throws Exception {
        String nama[] = { "Ani", "Budi", "Wati", "Guntur", "Yanti" };
        for (int i = 0; i < nama.length; i++) {
         System.out.println(nama[i]);
        }
    }
}
